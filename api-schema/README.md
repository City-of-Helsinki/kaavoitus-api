# Generate API-schema

Unversioned:
```bash
./manage.py spectacular \
  --file api-schema/schema.yaml
```

V1:
```bash
./manage.py spectacular \
  --file api-schema/schema-v1.yaml \
  --api-version=1
```
